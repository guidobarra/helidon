server:
  port: 9292
  host: 0.0.0.0

app:
  name: '@project.artifactId@'
  version: '@project.version@'
  environment: dev

db:
  source: jdbc
  connection:
    url: jdbc:postgresql://localhost:5432/sales
    username: helidon
    password: helidon123
    poolName: helidonPool
    maximumPoolSize: 10
  statements:
    find-all: "SELECT * FROM users ORDER BY id"
    find-by-id: "SELECT * FROM users WHERE id = ?"
    find-by-email: "SELECT * FROM users WHERE email = ?"
    insert: "INSERT INTO users (name, email) VALUES (?, ?)"
    update: "UPDATE users SET name = ?, email = ? WHERE id = ?"
    delete: "DELETE FROM users WHERE id = ?"

